# Plugins

This directory stores optional plugins. The registry is tracked in [`registry.json`](./registry.json).

## Registry entry fields
- `id`: Unique plugin identifier.
- `path`: Relative path to the plugin directory.
- `description`: Short description for humans.

If you add a new plugin, add a matching entry to `registry.json`.
